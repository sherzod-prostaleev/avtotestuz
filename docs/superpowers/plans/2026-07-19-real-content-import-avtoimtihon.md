# Real Content Import — avtoimtihon-format dataset → canonical import

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Replace/supplement the `[NAMUNA]` fixture data with **real, user-owned** question content (1235 questions, 3 languages, 716 images) sourced from `/home/sher/Рабочий стол/aaa/` (the user's own earlier prototype, confirmed by the user to be content they have rights to use — not a live-scrape, satisfies spec D5). This unlocks the actual product for the first time — everything after this plan works against real exam content instead of sample data.

**Context (facts gathered and confirmed with the user before writing this plan):**
- Source: `/home/sher/Рабочий стол/aaa/src/data/questions.{uz-Latn,uz-Cyrl,ru}.json`, each an array of 1235 objects: `{id, question, image?, comment?, answers: string[], correct_answer: 1-based int, ticket: string}`. IDs 1-1235, identical across all 3 locale files (verified). Images live at `/home/sher/Рабочий стол/aaa/public/quiz-images/<image>.webp` (716 unique refs, 1 missing file — `i120_9`, confirmed absent from the whole `aaa/` tree, not just `public/`).
- Source "tickets" (124 of them) have **10 questions each** (123 tickets) with a final 125th... no — **123 full tickets of 10 + 1 partial ticket (ticket "124") of 5** = 1235 total. Answer counts are **variable: 2/3/4/5** (not always 4). No `category` field exists in the source at all. No signs-catalog data exists in this source (question images are traffic-scenario photos, not a curated sign catalog).
- **User's explicit decision (must not be altered)**: keep answer counts exactly as source — 2, 3, 4, or 5, whatever each question actually has. This is real exam data; do not force/pad/trim to 4.
- **User's explicit decision**: bilets (variants) in OUR system must be **20 questions each**, matching the real official exam format (master spec's own "Tekshirilgan faktlar": rasmiy imtihon 20 savol) — achieved by **pairing two consecutive source tickets into one bilet** (source ticket 1+2 → our variant 1, source ticket 3+4 → our variant 2, etc.). Confirmed arithmetic: tickets 1-122 (122 tickets, all size 10) pair cleanly into **61 variants of exactly 20 questions**. Source tickets 123 (10) + 124 (5) = **15 leftover questions that do NOT form a clean 20-question pair** — these 15 questions are still imported as valid, usable questions (practice/mistakes/FSRS all still work on them) but are **not assigned to any numbered variant/bilet**. This requires **zero change** to the existing "exactly 20 questions per variant" backend invariant — only the per-question answer-count invariant needs to relax.
- Category: since the source has none, every imported question gets a single fallback category, code `umumiy` (general/uncategorized) — a real, honest limitation (FSRS category-level mastery breakdown will show one bucket until real categorization is added later), not silently hidden. Flagged in this plan's Self-Review, not treated as solved.
- Explanation: source's flat `comment` string maps to a single explanation block per question (using the CORRECT canonical schema — `text` field, type `muhim` — matching the schema fix landing in a parallel task; verify that fix has landed before this plan's Task B ships, since using the stale `md`/`important` shape here would repeat the exact bug just found). 16 questions (1.3%) have no `comment` — those get no explanation at all (no explanation row), not a fabricated one.
- Correct-answer indexing: source `correct_answer` is **1-based** (confirmed: range is 1..5, matching `len(answers)`, never 0). The canonical `CanonAnswer.Position` is also 1-based (`json:"position"`) — direct mapping, no off-by-one adjustment needed, but VERIFY this yourself before assuming, per this plan's own "don't guess" discipline.

## Global Constraints

- Repo root: `/home/sher/Рабочий стол/avtotest` (Cyrillic + space — always double-quote, never backslash-escape).
- **Depends on a parallel fix**: `backend/internal/fixture/fixture.go`'s explanation blocks were found using a stale `md`/`important` schema instead of the canonical `text`/`muhim` schema (from `backend/internal/explanation/draft.go`). Confirm that fix has landed (check `git log` for a commit fixing this, or re-read `fixture.go` yourself) before writing Task B's explanation-block construction — use whatever the corrected canonical schema actually is, don't reintroduce the bug.
- Canonical import format is `backend/internal/importer/format.go`'s `Dataset` struct — read it yourself, it's the authoritative target shape (categories/sign_groups/signs/questions/variants/explanations, with `CanonQuestion.Texts`/`CanonAnswer.Texts` keyed by locale, `CanonAnswer.Position` 1-based, `CanonVariant.Questions` as an ordered `ext_id` list).
- Validation is `backend/internal/importer/validate.go` — read it yourself before Task A; know exactly which check to relax (line ~38: `len(q.Answers) != 4`) and which to deliberately leave untouched (line ~80: `len(v.Questions) != 20` — stays exact, since the converter itself guarantees exactly-20 groupings and simply omits the 15 leftover questions from any variant).
- Storage/import entrypoint is `backend/cmd/importer` (flags include `-data`, `-verified` — read `main.go` for the exact interface) backed by `internal/importer.Store` (`StoreOptions{MarkVerified, Images, Source}`) — this is real, licensed content per the user's confirmation, so importing with `-verified`/`MarkVerified: true` is appropriate (mirrors how the `[NAMUNA]` fixture itself is imported with `MarkVerified: true` in tests).
- Images: source files are `.webp` under `aaa/public/quiz-images/`. The importer's `ImageSource.Open(rel)` (check `internal/blob`) resolves paths relative to a dataset directory — the converter must either copy/symlink images into the output dataset dir's expected layout, or otherwise satisfy whatever `ImageSource` implementation `cmd/importer` uses (`blob.NewLocalDir` for dev, per Plan 01's precedent) so image bytes are actually reachable at import time.
- This plan is backend-only (Go) — it can run in parallel with Plan 07 (Flutter) without file conflicts, and should NOT block Plan 07's execution.
- Testing: Task A needs unit tests for the relaxed validator (2/3/4/5 valid, still rejects 0/1 and >5 if you choose a sane upper bound — confirm what the real data's actual max is, which is 5, and don't invent a higher ceiling than what's evidenced). Task B needs a test or a clear manual verification pass proving the conversion is faithful (spot-check specific known questions/images end-to-end, not just "it ran without erroring").

---

### Task A: Relax the answer-count invariant

**Files:** modify `backend/internal/importer/validate.go`, `backend/internal/importer/validate_test.go` (or wherever its tests live — check first).

**Logic:** Change `len(q.Answers) != 4` (validate.go ~line 38) to accept a range matching the real data's evidenced minimum/maximum — 2 to 5 inclusive (confirm these bounds against the actual source data yourself: `python3 -c "..."` over the three JSON files, don't just trust this plan doc's numbers, they could be stale by the time you run this). The `correct == 0`/`correct > 1` check (currently gated behind `if len(q.Answers) == 4`) must apply regardless of answer count — a question with 2, 3, 4, or 5 answers must still have exactly 1 marked correct; fix the gating condition accordingly (it should probably just always run once the count itself is valid, not be conditional on being exactly 4).

Do NOT touch the `len(v.Questions) != 20` variant-size check — that stays exactly as-is (see Global Constraints for why).

**Testing:** Update/add validator tests covering: a question with 2 answers/1 correct → valid; 3 answers/1 correct → valid; 5 answers/1 correct → valid; 1 answer → invalid (`answers_count`); 6 answers → invalid; any valid-count question with 0 or 2+ correct answers → invalid (`no_correct`/`multiple_correct`) regardless of which valid count it has. Also confirm the EXISTING `[NAMUNA]` fixture (which uses 4 answers) still imports cleanly — run the full backend suite.

- [ ] Steps: read validate.go + its test file fully, confirm the real data's min/max from the actual source JSON, implement, test, run `cd backend && go build ./... && go test ./... -p 1 -count=1` (may need `make up` first for DB-touching tests).
- [ ] Commit: `git add backend/internal/importer/` then `git commit -m "feat(backend): allow 2-5 answers per question (was strict 4) for real licensed content"`.

---

### Task B: avtoimtihon → canonical dataset converter

**Files:** create a new command, e.g. `backend/cmd/convertavtoimtihon/main.go` (mirroring the existing `cmd/importer`/`cmd/genfixture` structure/conventions — read one of them first for the established CLI/error-handling style), plus a small internal package if the transform logic is substantial enough to warrant testing in isolation (your call — `internal/importer` itself might be the right home for pure transform helpers if that fits better than a bespoke package).

**Logic — read this carefully, it encodes real decisions already made, don't re-derive or second-guess them:**

1. Read all 3 source JSON files from `/home/sher/Рабочий стол/aaa/src/data/questions.{uz-Latn,uz-Cyrl,ru}.json`. Confirm (assert, don't just assume) that all 3 have identical, aligned `id` sequences — fail loudly if not.
2. Build one `CanonQuestion` per source `id`, with `ext_id` = e.g. `"avtoimtihon-<id>"` (a stable, source-tagged ext_id — don't reuse bare numeric IDs that could collide with any other future import), `category` = `"umumiy"` (constant, every question), `texts` = `{uz-Latn: ..., uz-Cyrl: ..., ru: ...}` from the 3 files' `question` field, `image` = the source's `image` field IF that image file actually exists on disk (see step 4 — if the referenced image is the one known-missing file `i120_9`, or any other missing file you discover, DO NOT set `Image` — quarantine-by-omission is wrong here; instead either fail loudly and ask, or (preferred, simpler) just emit the question with no image and log a warning — a driving-theory question missing its one referenced photo should still be usable via its text, don't drop the whole question over one missing asset), `answers` = built from the 3 files' `answers[]` arrays (same length across all 3 locales — verify this per-question, fail loudly if a length mismatch is found across languages for the same id), `correct` flag set on whichever answer index equals `correct_answer - 1` (0-based array index from the 1-based source value — verify this conversion is right with a few hand-checked examples), `source` = `"avtoimtihon"`.
3. **Ticket pairing → variants**: group source tickets by their numeric value (parse `ticket` string as int), sort ascending. Pair consecutive tickets (1&2, 3&4, ..., n-1&n) into one `CanonVariant` each, `Number` = 1, 2, 3, ... (sequential, don't reuse source ticket numbers), `Questions` = the 20 ext_ids in original order (first ticket's 10 questions, then the second ticket's 10, preserving each ticket's internal ordering). If the total ticket count is odd or the last ticket(s) don't form a clean pair-of-two-size-10 (compute this for real from the actual data, don't assume the 123+1-partial shape this plan doc describes is still accurate), the leftover questions are simply not included in any `CanonVariant` — they still exist as valid standalone `CanonQuestion` entries. Log exactly which ext_ids ended up unassigned so this is auditable, not silent.
4. **Images**: for every unique image filename referenced, locate the actual file under `/home/sher/Рабочий стол/aaa/public/quiz-images/` (check the real extension — `.webp` per what you found; there might be others, verify). Copy (don't symlink across the Cyrillic-path boundary if that's ever finicky — just copy) each into whatever directory structure the importer's `ImageSource`/`blob.NewLocalDir` expects relative to the dataset's own output directory (check `cmd/importer/main.go` and `internal/blob`'s local-dir implementation for the exact expected relative-path convention — don't guess). Track and report any image referenced-but-not-found (expect exactly 1: `i120_9`, but verify, don't assume the count hasn't changed).
5. **Explanations**: for every question with a non-empty `comment` (in ANY of the 3 locale files — a question might have a comment in one locale and not another; decide and document a sane per-locale fallback, e.g. if uz-Latn has a comment but ru doesn't, either duplicate the uz-Latn comment into ru with a note, or omit the ru block for that question — your call, but make it deliberate and log it, don't silently produce inconsistent locale coverage without recording it), build one `CanonExplanation` per question with a single-block-per-locale structure using the CORRECTED canonical schema (confirm the exact field/type names from the now-fixed `fixture.go`/`draft.go` — do not use `md`/`important`). `LegalRefs` — the source `comment` text often already contains inline legal citations (e.g. "YHQ 1-ilovasi 3.27...") as prose; you do NOT need to parse/extract structured `legal_refs` entries from this prose for this pass — leave `LegalRefs` empty/nil rather than fabricating a structured citation your extraction might get wrong. That's an honest gap, not silently invented data.
6. Categories dataset needs exactly one entry: `{code: "umumiy", sort: 0, names: {uz-Latn: "Umumiy savollar", uz-Cyrl: (Cyrillic transliteration), ru: "Общие вопросы"}}`.
7. No `sign_groups`/`signs` in this dataset — leave those arrays empty (this import doesn't touch the existing `[NAMUNA]` signs catalog at all, if the goal is to ADD this content alongside existing categories/signs rather than wipe them — confirm with `cmd/importer`'s actual behavior: does importing a new dataset wipe prior categories/signs, or additively upsert? Read `internal/importer/store.go` to know for certain before running anything against a real dev DB you care about).
8. Write the resulting `Dataset` as JSON to an output path (e.g. `backend/seed/avtoimtihon/data.json` plus its `images/` dir) — check `.gitignore` conventions for generated seed data (the existing `[NAMUNA]` seed output is gitignored per `backend/seed/sample/` in `.gitignore` — decide whether this new real-content output should be committed (it's real licensed content now, arguably fine to commit, unlike synthetic fixture output) or also gitignored (simpler, keeps the repo from carrying 700+ image blobs) — lean toward gitignored + a documented regeneration command, matching the existing pattern, unless there's a strong reason to commit binary content.

**Testing:** Run `internal/importer.Validate` against the converter's own output BEFORE attempting a real DB import — confirm zero unexpected issues (some `variant_size`-type "issues" for the 15 leftover questions are fine/expected if they show up as informational, but there should be NO `answers_count`/`no_correct`/`multiple_correct`/`missing_locale`/`unknown_category` issues on any of the 1235 questions, since this is supposedly clean real data — if any DO show up, that's a genuine data-quality finding worth surfacing, not silently quarantining and moving on). Write this validation as an actual Go test or at minimum a documented one-off run with real output captured in your report — don't just assert "it validated fine" without showing the numbers (issue count, quarantined count — should be 0 quarantined given the source data's confirmed cleanliness).

- [ ] Steps: read `cmd/importer/main.go`, `internal/blob`, `internal/importer/store.go` first. Build the converter. Run it. Run `Validate` against its output and report exact numbers. Commit the converter code (not necessarily its output, per the gitignore decision above).
- [ ] Commit: `git add backend/cmd/convertavtoimtihon/` (+ whatever else) then `git commit -m "feat(backend): avtoimtihon dataset converter — real content → canonical import format"`.

---

### Task C: Import real content into dev DB + verify + document

**Files:** no new source files expected; this is a verification/documentation task. Possibly `Makefile` (a new `make seed-real` target, if that fits the existing convention) and `README.md`.

**Logic:**
1. `make up` (repo root) if not already running.
2. Run the actual import: `cd backend && go run ./cmd/importer -data <converter's output dir> -verified` (or whatever Task B's converter actually produces/whatever flag shape `cmd/importer` really has — confirm from its `main.go`). Capture the full `Report` output (categories/signs/questions/variants/images counts, any quarantine).
3. **Decide and confirm with actual behavior** (don't assume) whether this ADDS to the existing `[NAMUNA]` seed data or requires a clean DB first — if `make seed` was already run in this dev environment, either truncate first (check `internal/testdb`'s truncate helper or a documented reset path) or confirm additive import is safe and intentional. Given the goal is "real content is what the app should show now," a clean re-seed (real content only, or real + fixture side-by-side clearly distinguishable) is probably right — use your judgment, but document exactly what you did and why.
4. Spot-check via the actual running API (start `PORT=8090 go run ./cmd/api` if not running): fetch `GET /api/v1/variants` (should show ~61 real variants, not [NAMUNA]'s 2), `GET /api/v1/variants/1?locale=uz-Latn` (20 real questions, check a couple of question texts/images against the known source `aaa/src/data/questions.uz-Latn.json` by `id` to confirm fidelity — pick 2-3 specific IDs and manually diff), `GET /api/v1/questions/{a real id}?locale=ru` (confirm Russian text is correct, confirm the image URL resolves to a real, viewable image — actually fetch the image bytes via curl and confirm it's a valid webp, not a 404).
5. Update `README.md`'s content section to describe the real dataset now available (source, question/variant counts, the `umumiy`-category and 15-leftover-questions limitations, honestly) — don't overwrite the existing `[NAMUNA]` fixture documentation if that's still separately relevant for backend test suites, just add real-content info alongside it.

**Testing:** This task's "testing" IS the live verification described above — capture real command output, not paraphrased.

- [ ] Commit: `git add Makefile README.md` (whatever changed) then `git commit -m "docs: real avtoimtihon content imported and verified (61 bilets, 1235 questions)"`.

## Self-Review

1. **Honest limitations, not silently hidden**: single fallback category (`umumiy`) for all real content — category-level FSRS mastery breakdown (Plan 04's `GET /me/stats`) will show one bucket for all real questions until proper categorization exists (a real product gap, worth a follow-up plan later, not solved here). 15 leftover questions (from odd-ticket-count pairing) exist but aren't part of any named bilet. No signs catalog comes from this import (the existing `[NAMUNA]` 4-sign catalog, or a future dedicated signs-content acquisition, remains the only signs data). `LegalRefs` are empty for all imported explanations (the source's prose already contains inline citations, just not machine-extracted into structured fields).
2. **Scope boundary**: this plan does NOT touch Flutter (Plan 07 continues independently), does NOT add a signs catalog, does NOT retroactively categorize questions, does NOT extract structured legal references from prose. All explicitly out of scope, not gaps to rediscover later in confusion.
3. **Depends on the fixture-schema fix**: Task B's explanation-block construction is only correct if the parallel fixture.go schema fix has already landed — verify this explicitly before writing that part of Task B, don't assume it happened just because it was dispatched.
