# golangci-lint v2 config. Defaults already include errcheck, govet,
# ineffassign, staticcheck, unused.
version: "2"
linters:
  enable:
    - misspell
  settings:
    misspell:
      # Period / Redis key token ("alltime"), not prose "all-time".
      ignore-rules:
        - alltime
