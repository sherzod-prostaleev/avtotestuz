# M2-11 Guest Demo Landing Funnel Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use `- [ ]`.

**Goal:** Enhance the landing page demo question block with conversion CTA cards after answering and seamless "Next demo question" iteration to convert guest traffic into registered users.

**Architecture:** Frontend Next.js component (`DemoQuestionBlock.tsx`), Tailwind CSS, i18n (`next-intl`), Vitest unit tests.

## Global Constraints
- `npm run typecheck` and `npm run test` must remain 100% green.
- 3 locales: `uz-Latn`, `uz-Cyrl`, `ru`.

---

### Task 1: Update `DemoQuestionBlock` & i18n
**Files:**
- Modify `frontend/messages/uz-Latn.json`, `uz-Cyrl.json`, `ru.json`
- Modify `frontend/src/app/[locale]/(public)/demo-question-block.tsx`
- Modify `frontend/src/app/[locale]/(public)/demo-question-block.test.tsx`

- [ ] Add conversion CTA strings (`demoSuccessTitle`, `demoSuccessBody`, `demoFailureTitle`, `demoFailureBody`, `demoRegisterCta`, `demoNextQuestion`) to i18n files.
- [ ] Update `DemoQuestionBlock` component to display conversion CTA card upon grading with signup redirect and next question retry button.
- [ ] Update component unit tests in `demo-question-block.test.tsx`.

---

## Verification Plan
- `npm run typecheck` clean.
- `npm run test` green.
