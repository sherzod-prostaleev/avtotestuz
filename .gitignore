# Reference material (raqobatchi skrinshotlari — onless.uz, osonprava.uz) — hozircha treklanmaydi
Снимок*
photo_*_2026-*.jpg

# run.sh working files
.run/

# Local store-size brand masters (Play/Telegram/Instagram 1024s). Chrome
# and PWA icons live under frontend/public; do not ship 1MB rasters in git.
assets/branding/

# Staging / Docker overlay secrets (template is deploy/app.env.example)
deploy/app.env
deploy/app.env.prod.generated

# Go
backend/bin/
backend/seed/sample/
backend/seed/avtoimtihon/images/
backend/var/
backend/*.log
.env
scratch/
.claude/
111.png

# Agent / IDE local-only (never deploy)
.agents/
.codex/
.mimocode/
.superpowers/
.worktrees/
.cursor/

# Accidental root tooling
/node_modules/
**/__pycache__/
*.pyc
**/.venv/

# Sign dataset: data.json is generated by cmd/gensigns; the images under
# images/ are vendored inputs and ARE tracked so the import is reproducible.
backend/seed/signs/data.json

# Build artifacts / large local media
backend/api
*.wmv

# Generated by backend/station/cmd/genwinres at image build time from
# backend/station/build/drivergo.ico -- an opaque object file nobody can
# review in a diff, so it is built, not committed.
backend/station/cmd/avtotest-station/rsrc_windows_386.syso
backend/station/cmd/avtotest-station/rsrc_windows_amd64.syso

# Station logs dropped in for support triage — evidence from a school PC,
# never repo content.
station*.log
